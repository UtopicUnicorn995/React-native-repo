create new project: npx create-expo-app@latest

BETTER OPTION FOR ME: create new project: npx create-expo-app app-name --template blank 

expo IP address and pc ip address 
change expo ip address
setx /M REACT_NATIVE_PACKAGER_HOSTNAME 192.168.1.9

start server with tunnel: npx expo start --tunnel
remove boilerplate: npm run reset-project

Text and views are native elements

react native have no CSS
inline styles
Stylesheet Objects


Views components only holds components -> Does not show them

Button component does not have a style prop
Text can have a child Text element but not a view

styles does not cascade
styles should be small letters

eas build --profile development --platform android

expo has a built in icons

custom fonts can